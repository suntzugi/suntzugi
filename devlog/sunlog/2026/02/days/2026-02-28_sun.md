---
date: "2026-02-28"
day: saturday
day_number: 8
author: sun
timezone: Europe/Paris
utc_offset: "+01:00"
allnighter_tail_from: "2026-02-27"
allnighter_tail_end: "01:53"
allnighter_tail_hours: 1.9
commits_manual: 7
lines_added: 548
lines_removed: 422
tags: [architecture, migration, scripts, publishing]
---
# Feb 28, Saturday — Day 8

**Session:** tail from Feb 27 session 2, ending 01:53

**Commits:** 7 | **Lines:** +548 / -422

## What happened

Content architecture migration — replaced the three-folder publish pipeline with a single-folder + frontmatter system.

- Migrated cards from `texts/cards/` to `content/cards/` with YAML frontmatter
- Migrated essay from `texts/drafts/` to `content/essays/` with frontmatter (`status`, `publish_at`)
- Created `site.yaml` — links config extracted from `content.md`
- Moved text lists to `assets/text/`, audio to `assets/audio/bgm/`
- Wrote new `scripts/sync.js` replacing `sync-fallback.js` — reads frontmatter + site.yaml, bakes into HTML
- Rewrote `scripts/auto-publish.js` — updates frontmatter in-place instead of moving files between folders
- Updated `index.html` — removed ~350 lines of runtime `content.md` fetching/parsing, everything now baked at build time
- Updated watch.js paths, pre-commit hook, GitHub workflow, docs
- Deleted `texts/`, `audio/`, `content.md`, `sync-fallback.js`
- Created GitHub issue #7 (closed), issue #8 (YAML→JSON future migration)

Essays now stay in one folder forever. Status changes happen in frontmatter, not by moving files. No more middleman manifest.

## Benchmarks: before vs after migration

| Metric | Before | After | Change |
|--------|--------|-------|--------|
| index.html | 148,505 bytes | 137,765 bytes | -7.2% |
| Total lines (all text files) | 7,372 | 7,186 | -186 lines |
| index.html lines | 3,854 | 3,566 | -288 lines |
| Content files total | 15,425 bytes | 7,233 bytes | -53% |
| Total files in repo | 44 | 42 | -2 |
| HTTP requests (page load) | 5 (2 blocking) | 3 (0 blocking) | -40% |
| Runtime JS removed | — | — | ~350 lines |
| Runtime functions removed | — | — | 12 functions |

### Runtime before
1. `index.html` (148KB)
2. `texts/suntzugi/content.md` (fetched + parsed — blocking)
3. `texts/txt_lists/manifest.json` (fetched — blocking)
4. `texts/txt_lists/amor.txt` (fetched)
5. `texts/txt_lists/paciencia.txt` (fetched)

### Runtime after
1. `index.html` (138KB, all content baked in — renders immediately)
2. `assets/text/amor.txt` (non-blocking override)
3. `assets/text/paciencia.txt` (non-blocking override)

### Removed JS functions
`loadContent()`, `parseContentMd()`, `applyCard()`, `applyEssays()`, `applyLinks()`, `applyAudioTracks()`, `parseLinksWithMeta()`, `parseMarkdownLinks()`, `extractField()` (browser copy), `mdInline()` (browser copy), `extractFootnotes()` (browser copy), `window.__updateTracks()`
